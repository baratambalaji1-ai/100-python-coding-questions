Question:
Write a Python program to calculate the factorial of a given number n.

Example:

Input: 5
Output: 120

Hint:
Factorial of n = n × (n-1) × (n-2) × ... × 1

For example:
5! = 5 × 4 × 3 × 2 × 1 = 120
